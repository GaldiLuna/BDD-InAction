package steps.java;

public class RegistrationService {
    public BaggageRegistration registerBaggage(String message) {
        // lógica de processamento simples (ajuste conforme necessário)
        return new BaggageRegistration(message);
    }
}
